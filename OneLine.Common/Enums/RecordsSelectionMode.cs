namespace OneLine.Enums
{
    /// <summary>
    /// Determines the selection mode whether is single or multiple when selecting records from a list.
    /// </summary>
    public enum RecordsSelectionMode
    {
        Single,
        Multiple
    }
}
