namespace OneLine.Enums
{
    /// <summary>
    /// Determines the application session storage whether it will use the local storage or the session storage. 
    /// </summary>
    public enum ApplicationSession
    {
        SessionStorage,
        LocalStorage
    }
}
