namespace OneLine.Enums
{
    /// <summary>
    /// Determines the component type to control custom behaviors as needed when using a single base view class.
    /// </summary>
    public enum ComponentType
    {
        Custom,
        DataView,
        FormView,
        OptionsDialogView
    }
}
