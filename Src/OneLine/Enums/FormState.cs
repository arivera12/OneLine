namespace OneLine.Enums
{
    /// <summary>
    /// Dtermines the form state wether is on create, edit, copy, delete, deleted, details or read only state.
    /// </summary>
    public enum FormState
    {
        Create,
        Edit,
        Copy,
        Delete,
        Deleted,
        Details,
        ReadOnly
    }
}
