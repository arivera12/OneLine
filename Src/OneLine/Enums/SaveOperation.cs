namespace OneLine.Enums
{
    /// <summary>
    /// Determines the save operation whether is add or update.
    /// </summary>
    public enum SaveOperation
    {
        Any,
        Add,
        Update
    }
}
